export const paths = {
  auth: {
    login: {
      path: '/auth/login',
      getHref: () => '/auth/login',
    }
  },
  app: {
    root: {
      path: '/',
      getHref: () => '/',
    },
    home: {
      path: '/',
      getHref: () => '/',
    }
  }
}
